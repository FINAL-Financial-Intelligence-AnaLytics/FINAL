# Финансовый RAG Система

Комплексная система финансового ассистента на основе RAG (Retrieval-Augmented Generation) для предоставления образовательных финансовых консультаций, диагностики и планирования целей.

## 📋 Содержание

- [Описание](#описание)
- [Архитектура](#архитектура)
- [Компоненты системы](#компоненты-системы)
- [Установка и настройка](#установка-и-настройка)
- [Использование](#использование)
- [Данные](#данные)
- [API и интеграция](#api-и-интеграция)
- [Деплой](#деплой)
- [Безопасность](#безопасность)

## Описание

Финансовая RAG система предоставляет интеллектуального ассистента для:

- **Финансовой консультации** - поиск ответов через RAG, объяснения простым языком, обучение финансовой грамотности
- **Финансовой диагностики** - опросники, профили расходов/доходов, определение финансового стиля
- **Планирования целей** - создание финансовых целей, планы достижения, отслеживание прогресса

### Возможности ассистента

✅ **Что ассистент МОЖЕТ делать:**
- Обучать финансовой грамотности
- Объяснять экономические понятия простыми словами
- Показывать примеры расчётов
- Давать структуры, чек-листы, алгоритмы
- Объяснять принципы работы инструментов

❌ **Что ассистент НЕ делает:**
- Не даёт персональных инвест-советов
- Не рекомендует продукты
- Не сравнивает "что лучше купить"
- Не даёт юридических или налоговых рекомендаций

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      RAG Система                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐   │
│  │   Данные     │───▶│  Эмбеддинги  │───▶│ Векторная БД │   │
│  │  (CSV/JSON)  │    │  (Embeddings)│    │  (Qdrant/    │   │
│  └──────────────┘    └──────────────┘    │   Chroma)    │   │
│                                          └──────────────┘   │
│                                                    │        │
│                                                    ▼        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                  RAG Retriever                       │   │
│  │  - Поиск релевантных документов                      │   │
│  │  - Ранжирование по релевантности                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                    LLM Client                        │   │
│  │  ┌──────────────────┐  ┌──────────────────┐          │   │
│  │  │ Консультация     │  │ Диагностика      │          │   │
│  │  └──────────────────┘  └──────────────────┘          │   │
│  │  ┌──────────────────┐                                │   │
│  │  │ Цели/Инвестиции  │                                │   │
│  │  └──────────────────┘                                │   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              LLM Provider (Mistral AI)               │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Компоненты системы

### 1. LLM Клиент (`app/llm_client/`)

Модульная система для работы с LLM провайдерами:

- **`base_client.py`** - Базовый класс с правилами работы ассистента
- **`financial_consultation.py`** - Модуль финансовой консультации
- **`financial_diagnosis.py`** - Модуль финансовой диагностики
- **`goals_investments.py`** - Модуль целей и инвестиций
- **`implementations/mistral_client.py`** - Реализация для Mistral API

### 2. Данные (`*.csv`)

Финансовые данные для RAG:

- `fincult_articles_by_categories_formatted.csv` - Статьи по финансовой грамотности
- `finuslugi.csv` - Финансовые услуги
- `rbk_finances.csv` - Финансовые новости и аналитика
- `sravni_ru.csv` - Сравнение финансовых продуктов

### 3. Конфигурация (`app/config.py`)

Централизованная конфигурация системы через переменные окружения.

## Установка и настройка

### Требования

- Python 3.8+
- Mistral API ключ
- Векторная БД (Qdrant, Chroma, Pinecone и т.д.) - опционально

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Mistral настройки
MISTRAL_API_KEY=your-mistral-api-key
MISTRAL_MODEL=mistral-large-latest

# RAG настройки
RAG_TOP_K=5
MAX_TOKENS=2000
TEMPERATURE=0.7

# Безопасность
ENABLE_REQUEST_VALIDATION=true
ADD_SAFETY_DISCLAIMER=true

# Векторная БД (пример для Qdrant)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=financial_rag
```

Или экспортируйте переменные:

```bash
export MISTRAL_API_KEY="your-mistral-api-key"
export MISTRAL_MODEL="mistral-large-latest"
```

## Использование

### Базовое использование LLM клиента

```python
from app.llm_client import (
    FinancialConsultationModule,
    FinancialDiagnosisModule,
    GoalsInvestmentsModule
)
from app.llm_client.implementations import MistralFinancialClient

# Инициализация LLM клиента (Mistral)
llm_client = MistralFinancialClient(
    model_name="mistral-large-latest",
    api_key=None  # Используется из переменной окружения MISTRAL_API_KEY
)

# Модуль финансовой консультации
consultation = FinancialConsultationModule(
    llm_client=llm_client,
    rag_retriever=None  # Подключите ваш RAG retriever
)

# Поиск ответа через RAG
answer = consultation.search_answer_via_rag(
    "Что такое финансовая подушка безопасности?"
)

# Объяснение термина простым языком
explanation = consultation.explain_simple_language("инфляция")

# Ответ по финансовой грамотности
literacy_answer = consultation.answer_financial_literacy(
    "Как правильно планировать бюджет?"
)
```

### Интеграция с RAG системой

Для полноценной работы RAG необходимо реализовать retriever:

```python
from typing import List, Dict
import qdrant_client
from qdrant_client.models import Distance, VectorParams

class FinancialRAGRetriever:
    def __init__(self, qdrant_host: str, qdrant_port: int, collection_name: str):
        self.client = qdrant_client.QdrantClient(
            host=qdrant_host,
            port=qdrant_port
        )
        self.collection_name = collection_name
    
    def retrieve(self, query: str, top_k: int = 5) -> List[Dict]:
        # Генерация эмбеддинга запроса
        query_embedding = self._generate_embedding(query)
        
        # Поиск в векторной БД
        results = self.client.search(
            collection_name=self.collection_name,
            query_vector=query_embedding,
            limit=top_k
        )
        
        # Форматирование результатов
        documents = []
        for result in results:
            documents.append({
                "content": result.payload.get("content", ""),
                "source": result.payload.get("source", ""),
                "score": result.score
            })
        
        return documents
    
    def _generate_embedding(self, text: str) -> List[float]:
        # Используйте модель эмбеддингов (например, Mistral embeddings)
        # или локальную модель (sentence-transformers)
        pass

# Использование
rag_retriever = FinancialRAGRetriever(
    qdrant_host="localhost",
    qdrant_port=6333,
    collection_name="financial_rag"
)

consultation = FinancialConsultationModule(
    llm_client=llm_client,
    rag_retriever=rag_retriever
)
```

### Подготовка данных для RAG

```python
import pandas as pd
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams

# Загрузка данных
df = pd.read_csv("fincult_articles_by_categories_formatted.csv")

# Инициализация клиента Qdrant
client = QdrantClient(host="localhost", port=6333)

# Создание коллекции
client.create_collection(
    collection_name="financial_rag",
    vectors_config=VectorParams(
        size=1024,  # Размерность эмбеддинга (для Mistral embeddings)
        distance=Distance.COSINE
    )
)

# Генерация эмбеддингов и загрузка в БД
for idx, row in df.iterrows():
    text = row['content']
    embedding = generate_embedding(text)  # Ваша функция генерации
    
    client.upsert(
        collection_name="financial_rag",
        points=[{
            "id": idx,
            "vector": embedding,
            "payload": {
                "content": text,
                "source": row.get('source', 'unknown'),
                "category": row.get('category', '')
            }
        }]
    )
```

## Данные

Проект включает следующие наборы данных:

### `fincult_articles_by_categories_formatted.csv`
Статьи по финансовой грамотности, структурированные по категориям.

### `finuslugi.csv`
Информация о финансовых услугах и продуктах.

### `rbk_finances.csv`
Финансовые новости и аналитические материалы.

### `sravni_ru.csv`
Данные для сравнения финансовых продуктов.

**Примечание:** Перед использованием данных убедитесь, что они соответствуют вашим требованиям лицензирования и использования.

## API и интеграция

### REST API (пример)

```python
from fastapi import FastAPI
from app.llm_client import FinancialConsultationModule
from app.llm_client.implementations import MistralFinancialClient

app = FastAPI()

llm_client = MistralFinancialClient(model_name="mistral-large-latest")
consultation = FinancialConsultationModule(llm_client=llm_client)

@app.post("/api/consultation/search")
async def search_answer(query: str):
    answer = consultation.search_answer_via_rag(query)
    return {"answer": answer}

@app.post("/api/consultation/explain")
async def explain_term(term: str):
    explanation = consultation.explain_simple_language(term)
    return {"explanation": explanation}
```

### Интеграция с существующими системами

Система может быть интегрирована через:

- REST API
- gRPC
- WebSocket для потоковой передачи
- Пакетный режим для обработки больших объемов запросов

## Деплой

### Docker

```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ ./app/
COPY *.csv ./

ENV PYTHONPATH=/app
CMD ["python", "-m", "app.example_usage"]
```

### Kubernetes

Конфигурации для деплоя находятся в директории `deploy/`.

### Переменные окружения для продакшена

```bash
MISTRAL_API_KEY=prod-mistral-api-key
MISTRAL_MODEL=mistral-large-latest
RAG_TOP_K=10
MAX_TOKENS=4000
TEMPERATURE=0.7
ENABLE_REQUEST_VALIDATION=true
ADD_SAFETY_DISCLAIMER=true
```

## Безопасность

### Валидация запросов

Система автоматически проверяет запросы на соответствие правилам работы ассистента:

- Блокировка персональных инвестиционных советов
- Предотвращение рекомендаций конкретных продуктов
- Фильтрация юридических и налоговых запросов

### Дисклеймеры

Все ответы автоматически дополняются дисклеймером о том, что информация носит образовательный характер.

### Рекомендации по безопасности

1. Храните API ключи в секретах (Kubernetes Secrets, AWS Secrets Manager)
2. Используйте HTTPS для всех API запросов
3. Реализуйте rate limiting для предотвращения злоупотреблений
4. Логируйте все запросы для аудита
5. Регулярно обновляйте зависимости

## Разработка

### Структура проекта

```
FINAL/
├── app/
│   ├── config.py                    # Конфигурация
│   ├── example_usage.py             # Примеры использования
│   └── llm_client/
│       ├── __init__.py
│       ├── base_client.py           # Базовый класс
│       ├── financial_consultation.py
│       ├── financial_diagnosis.py
│       ├── goals_investments.py
│       └── implementations/
│           ├── __init__.py
│           └── mistral_client.py
├── deploy/                          # Конфигурации деплоя
├── *.csv                           # Данные для RAG
├── requirements.txt                # Зависимости
└── README.md                       # Документация
```

### Запуск примеров

```bash
cd app
python example_usage.py
```

### Тестирование

```bash
pytest tests/
```

## Лицензия

[To be written]

## Контакты и поддержка

[To be written]

